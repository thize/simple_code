name: simple_code
description: Make Development Faster and Easier
version: 4.0.5
homepage: https://github.com/thize/simple_code

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  bordered_text: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  lint: ^1.2.0

flutter: